# API
PORT=4000
JWT_SECRET=change-me-to-a-long-random-string
# Optional: override DB path (default: server/data.db)
# DATABASE_PATH=./server/data.db

# Frontend (Vite) — used at build/dev time
VITE_API_URL=http://localhost:4000
VITE_RESTAURANT_NAME=Nonna's Kitchen
VITE_TAGLINE=Homemade Italian, Made Fresh Daily
